import React from "react";
import { ScrollView, View, Text } from "react-native";

export default function History({ navigation }) {
    return (
        <View>
            <ScrollView>
                <Text> Hi im in History </Text>
            </ScrollView>
        </View>
    );
}